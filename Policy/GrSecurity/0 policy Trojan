# Grsecurity ha un unico blocco per le policy
# directory o flag di Pax interessati
# ..

# di stock troviamo /tmp settato solo in lettura e scrittura, quindi questo bloccava l'attacco
/ tmp rw

# Per far si che l'attacco funzionava dovevo aggiungere alla policy stock, a /tmp il permesso di esecuzione, con x e quello per copiare il bit suid con m
   /tmp  rwxm


####### variante 2

# anche la variante 2 è bloccata di default, in quanto non è concesso nella home o qualsiasi altra directory, di far copiare il bit suid
/home rwcd
# dove c e d stanno per la possibilità di creare e rimuovere directory;

# per far si che l'attacco funziona si doveva impostare /tmp nel seguente modo:
/home rwxcdm
